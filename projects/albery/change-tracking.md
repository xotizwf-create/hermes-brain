---
id: albery-change-tracking
type: project
project: albery
tags: [albery, process, bitrix, change-log, engineering-discipline]
updated: 2026-07-16
secret_refs: []
---

# Albery — каждое изменение = закрытая задача в Битриксе (инженерная дисциплина)

Стоячее требование владельца (2026-07-13): **любое изменение в работающей системе Albery
ведётся как отдельная задача в Битрикс24 — с полным контекстом, ссылкой на коммит и временем,
чтобы был виден хронологический порядок.** Работаем как инженеры топовых компаний: каждое
изменение прослеживаемо и к нему можно вернуться. Это НЕ замена changelog'у мозга — это
дополнительный, видимый команде журнал в самом Битриксе.

## Когда заводить задачу

Одна задача = **одно атомарное изменение = один коммит** в репозитории Albery
(`github.com/xotizwf-create/Albery`). Заводить при изменении **работающей системы**:
код (`b24bot.py`, `mcp/context_server.py`, модули), конфиг (`/root/.hermes/config.yaml`, `.env`,
systemd), инструкции агента в БД (Маршрутная карта и пр.), `SOUL.md`, промпты, миграции.

Не нужно заводить задачу на чисто документационные правки мозга (`projects/albery/*.md`,
память) — они и так есть в `logs/changelog.md`. Но если правка инструкций **меняет поведение
агента на проде** (например, Маршрутная карта, SOUL) — это изменение системы, задача нужна.

## Поля задачи (шаблон)

| Поле | Значение |
|---|---|
| **Ответственный** | Александр Никитенко, `bitrix_user_id=16` |
| **Постановщик (creator)** | ИИ Агент, `bitrix_user_id=22` |
| **Дедлайн** | момент выполнения работы (МСК) — задаёт хронологию; несколько изменений подряд идут с возрастающим временем |
| **Название** | `Albery: <суть изменения одной строкой>` |
| **Описание** | контекст: что за проблема, КТО столкнулся/сообщил, симптом, первопричина (root cause) |
| **Критерий результата** (`result_criteria`, обязателен) | что считается выполненным |
| **Комментарий-результат** (при закрытии) | что именно сделано + коммит + проверка + откат |
| **Статус** | сразу закрыта (работа уже сделана и проверена) |

## Как ПИСАТЬ задачу (стандарт качества, требование владельца 2026-07-14)

**Задача — это инженерный документ, а не пересказ чата.** Правила:

1. **НИКОГДА не копируй запрос владельца в задачу.** Его слова («сделай, чтоб не тупило»,
   «что за фигня») — это симптом. Переформулируй в инженерную постановку: что именно сломано,
   у кого проявилось, чем это плохо для бизнеса.
2. **Название** — суть изменения, а не жалоба. Плохо: «Агент тупит с ценами». Хорошо:
   «Центр Агента: полный журнал переписок — видны все сообщения агентов, включая проактивные».
3. **Структура описания** (BB-разметка, разделы):
   `[b]Контекст[/b]` — как система работала и почему так было спроектировано →
   `[b]Следствия[/b]` — что именно сломалось, с КОНКРЕТНЫМИ примерами (задача/диалог/цитата) →
   `[b]Требование[/b]` — что должно быть.
4. **Комментарий-результат** — тоже разделами: `[b]Реализовано[/b]` (нумерованный список того,
   что сделано, с механикой) → `[b]Проверка[/b]` (что и как проверено, с цифрами) →
   `[b]Откат[/b]` (путь к бэкапу / `git revert <hash>`). Обязательна ссылка на коммит.
5. **Цифры и цитаты вместо оценок.** Не «стало лучше», а «39/43 вместо 14 за 120 с»;
   не «агент ошибался», а цитата: «Пришлите скрины отзывов» — при 12 уже приложенных скринах.
6. **Эталон — задача 1448** («Центр Агента: полный журнал переписок»). Новые задачи оформлять
   по её образцу.

## Что значит «полный контекст» (чек-лист)

Описание + комментарий-результат вместе должны содержать ВСЁ, чтобы через полгода понять
изменение без раскопок:

1. **Проблема и кто столкнулся** — симптом словами пользователя, кто сообщил (Софья/Наталья/…),
   когда, номер диалога/задачи если есть.
2. **Root cause** — настоящая причина, не пересказ симптома.
3. **Что сделано** — конкретные действия, затронутые файлы/модули.
4. **Ссылка на коммит** — хеш + полная ссылка:
   `https://github.com/xotizwf-create/Albery/commit/<hash>`. Если правка не в git
   (конфиг/БД-инструкция) — указать файл и бэкап.
5. **Как проверено** — что за проверка боем/тест, какой результат (цитата вывода).
6. **Откат** — путь к бэкапу (`/root/albery-backups-YYYYMMDD/…`, `*.bak-…`) или `git revert <hash>`.
7. **Связь с другими** — если изменение продолжает серию, сослаться на предыдущую задачу.

## Хронология

- Задачи создаются **в том порядке, в каком делались изменения**; дедлайн = время работы, поэтому
  сортировка по сроку/дате создания сразу даёт правильную последовательность.
- Серия изменений за сессию — отдельные задачи, каждая со своим коммитом и временем; в описании
  можно указать «шаг N сессии от <дата>».

## Как создавать (технически, проверено 2026-07-13)

Задачи создаются напрямую вызовом MCP-инструментов из отдельного скрипта на проде (импорт
`mcp.context_server` подгружает `.env`). Полный рабочий скаффолд был в этой сессии; ключевое:

```python
import os, sys
os.environ["B24_TASK_OFFER"] = "0"           # ВАЖНО: глушим оффер-комментарий на этих готовых задачах
sys.path.insert(0, "/var/www/albery")
from mcp import context_server as cs

created = cs.tool_create_bitrix_task({
    "title": "Albery: …", "description": "…",
    "responsible_bitrix_user_id": 16, "creator_bitrix_user_id": 22,
    "deadline": "2026-07-13T18:00:00+03:00",   # время работы
    "result_criteria": "…",
})
tid = created["task_id"]
cs.tool_complete_bitrix_task({
    "bitrix_task_id": tid, "on_behalf_bitrix_user_id": 22,
    "expected_title": "Albery: …",
    "result_text": "СДЕЛАНО (Albery <hash>):\n- …\nПроверка: …\nОткат: …",
})
```

### Грабли и правила (набито в этой сессии)

- **Глушить оффер И планировщики:** `B24_TASK_OFFER=0` и `B24_TASK_CHECKIN=0` до импорта —
  иначе на готовую задачу прилетит оффер «могу помочь», а импорт `mcp.context_server` тянет
  `agent_center` и СТАРТУЕТ живой планировщик чекина (14.07 обход 12:00 запустился из фонового
  офлайн-скрипта; клейм по дате спас от дубля, но полагаться на это нельзя).
- **Проверять результат по `comment_id` из ответа инструмента** (`tool_complete_bitrix_task` →
  `result.comment_id`; с 16.07.2026, Albery `c6195fb`, инструмент сам падает, если портал не вернул id —
  до этого он тихо отдавал `comment_added=true` без id, и результат мог пропасть). Ридеры ненадёжны:
  сырой REST `task.commentitem.getlist` отдаёт **ложный ноль**, и `cs.tool_get_task_comments` тоже
  показывал 0 для свежесозданного комментария, который точно существует (проверено 16.07.2026:
  add вернул id 22800, ридер — 0 даже спустя минуты). Есть id — комментарий есть.
- **Комментарии на этом портале НЕ удаляются и НЕ редактируются через REST** — и вебхуком, и
  app-токеном возвращается `ACTION_NOT_ALLOWED`. Если в задачу попал лишний/тестовый комментарий:
  **удалить всю задачу целиком** (`tasks.task.delete`, работает через вебхук) и создать заново
  начисто. Поэтому: не «тестировать» на боевой задаче — сначала отладить скрипт, потом один
  чистый прогон.
- `result_criteria` обязателен (инструмент откажет без него). Дедлайн в прошлом требует
  `confirm_past_deadline=true` — при записи «задним числом» указывать реальное время работы; если
  оно уже прошло, передать этот флаг.
- Закрытие с `result_text` само постит комментарий-результат с префиксом «✅ РЕЗУЛЬТАТ ЗАДАЧИ:»
  и закрывает задачу (`STATUS=5`, `STATUS_CHANGED_BY=22`).

## Пример — сессия 2026-07-13 (4 изменения)

Порядок и задачи этой сессии (все на Александра 16, дедлайн 13.07 18:00, закрыты):

1. `1380` — агент не читал документы (Excel/Word/PDF) и российские сайты — Albery `69d1028`.
2. `1374` — агент пересобирал присланные документы вместо точечной правки — Albery `c226768`.
3. `1376` — ходы агента зависали на 10 минут (таймаут) — Albery `4b818b4`.
4. `1378` — агент отвечал в задачах без вызова (только по @) — Albery `f571824`.

(Первая задача пересоздавалась 1372→1380: в 1372 попал тестовый комментарий, а REST не даёт его
удалить — удалил задачу целиком и создал заново. Отсюда правило «не тестировать на боевой».)

## 2026-07-30 — автор переноса сделок ИУ закреплён за ИИ-агентом

- **Проблема:** владелец сообщил, что сделки в воронке снова переносит случайный сотрудник,
  а не ИИ-агент.
- **Root cause:** обработчик сохранял OAuth из каждого события Bitrix. Такой токен принадлежит
  сотруднику, вызвавшему событие, поэтому следующий фоновый CRM-вызов получал случайного автора.
  `CREATED_BY` / `MODIFY_BY` / `MOVED_BY` в Bitrix доступны только для чтения и вычисляются из
  учётных данных REST-вызова.
- **Исправление:** обычные события больше не перезаписывают постоянную OAuth-цепочку; install/update
  дополнительно защищены постоянным `application_token`. Каждая CRM-мутация проверяет `user.current`
  тем же токеном и допускается только для `CRM_AGENT_USER_ID=22`. Сохранённая OAuth-цепочка
  пользователя 22 восстановлена и подтверждена на проде.
- **Коммит:** `492d5486729a156310681e7ff15a7aafa8a53600` —
  https://github.com/xotizwf-create/Albery/commit/492d5486729a156310681e7ff15a7aafa8a53600
- **Проверка:** 1690 passed, 28 skipped локально; CI Python 3.10/3.12 + PostgreSQL 14/16,
  frontend и security audit зелёные. Продовая тестовая сделка 304 создана, перенесена
  `C16:NEW → C16:UC_ANKETA` и удалена; `CREATED_BY_ID=22`, `MODIFY_BY_ID=22`,
  `MOVED_BY_ID=22`, после удаления поиск вернул 0 строк. `deploy_smoke.py`: `SMOKE OK`;
  `journalctl -u albery.service -p err`: пусто.
- **Задача Bitrix:** №2362, комментарий результата №37564.
- **Бэкап:** `/var/backups/albery/code/pre-crm-agent-auth-20260730_170322.tar.gz`.
- **Откат:** `git revert 492d548`.

## 2026-07-30 — вопрос клиенту перед передачей заполненной анкеты менеджеру

- **Сценарий:** после получения анкеты бот спрашивает, остались ли вопросы по подключению,
  и показывает отдельные кнопки `Да` / `Нет`.
- **Поведение:** `Да` возвращает главное меню и не уведомляет менеджера; `Нет` сообщает
  о подключении менеджера, бессрочно передаёт ему диалог и после доставки отправляет в
  действующую группу прежнее уведомление о заполненной анкете. Повторное нажатие
  «Присоединиться к ИУ» при уже заполненной анкете также вызывает менеджера.
- **Защита состояний:** кнопки анкеты отделены от `✅ Да` / `❌ Нет` подтверждения выхода
  из поддержки, поэтому ответ на анкетный вопрос не меняет режим поддержки.
- **Коммит:** `67f00d87c96c462324cc8a4b24c4cefc41078a34` —
  https://github.com/xotizwf-create/Albery/commit/67f00d87c96c462324cc8a4b24c4cefc41078a34
- **Проверка:** локально 1695 passed, 28 skipped; сценарно 108 passed; CI main (Python
  3.10/PostgreSQL 14, Python 3.12/PostgreSQL 16, frontend, Security audit) зелёный.
  На проде `deploy_smoke.py`: `SMOKE OK`; read-only smoke автомата: `POST-FORM LOGIC
  SMOKE OK`; очереди 0, три сервиса active, свежие error-журналы пусты.
- **Задача Bitrix:** №2362, комментарий результата №37720.
- **Бэкап:** `/var/backups/albery/code/pre-post-form-question-20260730_175441.tar.gz`.
- **Откат:** `git revert 67f00d8`.

## 2026-07-30 — постоянный выбор после просмотра заполненной анкеты

- **Сценарий:** каждое нажатие «Присоединиться к ИУ» при заполненной анкете заново
  показывает актуальные поля сделки и кнопки `Позвать менеджера` / `Перейти в меню`.
- **Поведение:** просмотр анкеты не создаёт уведомление и не меняет управление диалогом.
  Явный вызов менеджера бессрочно передаёт ему чат и создаёт уведомление `client_called`;
  переход в меню просто возвращает четыре основные кнопки.
- **Изоляция:** выбор хранится отдельным durable-событием и не пересекается с вопросом
  `Да` / `Нет` после заполнения анкеты или подтверждением выхода из поддержки.
- **Коммит:** `af9c23d7f0a392bf5126c9081ab305d294e45fde` —
  https://github.com/xotizwf-create/Albery/commit/af9c23d7f0a392bf5126c9081ab305d294e45fde
- **Проверка:** локально 1698 passed, 28 skipped; точечно 144 passed; CI main и Security
  audit зелёные. На проде `deploy_smoke.py`: `SMOKE OK`; read-only smoke:
  `FILLED-FORM CHOICE SMOKE OK`; очереди 0, три сервиса active, error-журналы пусты.
- **Задача Bitrix:** №2362, комментарий результата №37798.
- **Бэкап:** `/var/backups/albery/code/pre-filled-form-choice-20260730_225512.tar.gz`.
- **Откат:** `git revert af9c23d`.

## 2026-07-30 — восстановление ответов в старых диалогах Telegram-бота ИУ

- **Проблема:** диалог Александра Никитенко оставался в `human` без назначенного менеджера
  после устаревшего автоматического handoff при повторном открытии заполненной анкеты.
  Возврат в меню показывал клавиатуру, но не возвращал управление ИИ. В нерабочее время
  повторное сообщение также могло попасть под дедупликацию единственного ночного ответа.
- **Исправление:** старый hold распознаётся только по точной удалённой причине, отсутствию
  назначенного менеджера и отсутствию более поздней обработки менеджером. После `/start`,
  `/menu`, «Задать вопрос» и «Перейти в меню» управление возвращается ИИ после постановки
  служебного ответа в outbox. Для повторного ночного сообщения добавлена безопасная подсказка
  с главным меню без повторного handoff.
- **Изоляция:** назначенные и уже обработанные менеджером диалоги не восстанавливаются
  автоматически. Уведомление менеджеру при адресном восстановлении Александра не создавалось.
- **Коммит:** `5a5ea84544d727d606d9849980b00534fdca805a` —
  https://github.com/xotizwf-create/Albery/commit/5a5ea84544d727d606d9849980b00534fdca805a
- **Проверка:** локально 1701 passed, 28 skipped; CI ветки и main, frontend и Security audit
  зелёные. На проде `deploy_smoke.py`: `SMOKE OK`; шесть контрольных очередей пусты,
  три сервиса active, свежие error-журналы пусты. Сообщение Александру: message 838,
  outbox 351, Telegram provider message 665, `sent`; после доставки `control_mode=ai`,
  `unread_count=0`.
- **Задача Bitrix:** №2362, комментарий результата №37800.
- **Бэкап:** `/var/backups/albery/code/pre-never-silent-20260730_222017.tar.gz`.
- **Откат:** `git revert 5a5ea84`.

## 2026-07-31 — сообщение из калькулятора переведено на актуальный сценарий анкеты

- **Проблема:** подготовленный калькулятором текст распознавался, но старый обработчик
  заполненной анкеты автоматически передавал диалог менеджеру. В нерабочее окно ответ
  подменялся уже отправленным ночным уведомлением и исчезал по дедупликации, поэтому клиент
  видел молчание, а диалог оставался в `human`.
- **Исправление:** калькуляторный CTA теперь выполняет тот же сценарий, что
  «Присоединиться к ИУ»: показывает актуальную заполненную анкету с выбором
  `Позвать менеджера` / `Перейти в меню` либо выдаёт персональную ссылку. Сам CTA менеджера
  не вызывает и обрабатывается даже в `human`.
- **Распознавание:** допускает изменение регистра и пунктуации, а также опечатку
  в приветствии, но требует обе узкие смысловые части про рассчитанную экономику ИУ и
  обсуждение подключения.
- **Изоляция:** автоматический возврат ИИ применяется только к точной старой причине
  неподхваченного калькуляторного handoff без назначенного менеджера.
- **Коммит:** `0a36715c26990fd48c37707f2b176e706a4cbbe2` —
  https://github.com/xotizwf-create/Albery/commit/0a36715c26990fd48c37707f2b176e706a4cbbe2
- **Проверка:** точечно 58 passed; расширенно 893 passed, 23 skipped; полностью
  1702 passed, 28 skipped. CI ветки и main, frontend и Security audit зелёные.
  На проде `deploy_smoke.py`: `SMOKE OK`; шесть очередей пусты, три сервиса active,
  свежие error-журналы пусты.
- **Адресное восстановление:** сообщение Александра №851 обработано новым сценарием;
  анкета доставлена как message 852 / outbox 358 / Telegram provider message 679,
  `sent`; после доставки `control_mode=ai`, `unread_count=0`, уведомления менеджеру нет.
- **Задача Bitrix:** №2362, комментарий результата №37802.
- **Бэкап:** `/var/backups/albery/code/pre-calculator-join-20260730_230821.tar.gz`.
- **Откат:** `git revert 0a36715`.
