---
id: albery-bitrix-bot
type: project
project: albery
tags: [albery, bitrix, imbot, chatbot, hermes, mcp]
updated: 2026-06-25
secret_refs: []
---

# Albery — ИИ-ассистент внутри Bitrix24 (чат-бот на Мозге Гермеса)

Бот в чате Bitrix24, мозг которого — **Albery-Гермес** (агент на 186), с доступом по
уровню личности: обычные сотрудники → read-only знания компании (FAQ-MCP), Евгений/Александр →
полный MCP (планируется). Канал = ещё один вход к тому же агенту (как Telegram), не отдельный агент
(см. урок про дробление: [[hermes-multi-agent-design]]).

## Текущее состояние (2026-06-15)
- **Стадия 2 (read-only) работает в проде**: сотрудник пишет боту → бот показывает «печатает…» →
  Мозг Гермеса отвечает по знаниям компании (FAQ-MCP) → 👍 на сообщении.
- **Портал `b24-0xrp3s.bitrix24.ru` — БОЕВОЙ** (решение владельца 2026-06-16): именно тут агент
  отвечает сотрудникам, миграция на damaska НЕ нужна. Бот уже доступен всем сотрудникам этого
  портала (обычные → FAQ read-only; Евгений/Александр → full MCP), отдельно «раскатывать на всех» не
  требуется. На этом портале REST/imbot открыт (в отличие от damaska, см. [[albery-bitrix-rest-subscription]]).
- **Готово**: реакции 👀→👍, tiered-доступ (Евгений/Александр → full MCP), жизненный цикл сессий
  (idle 8ч + ротация со сводкой), лог взаимодействий + еженедельный дайджест Александру в Telegram.

## Архитектура
`Bitrix-чат (локальное приложение, imbot) → обработчик в Albery (app.py, на 186) → Мозг Гермеса
(hermes CLI на 186) → текст → обработчик постит ответ в чат`.

Петлю в Bitrix замыкает **обработчик** (Гермес умеет доставлять ответ только в встроенные платформы
telegram/slack/…, Bitrix среди них нет).

## Почему локальное приложение, а не вебхук
Bitrix **запрещает регистрировать чат-бота из входящего вебхука** (`imbot.register` →
`403 ACCESS_DENIED: "Client ID not specified"`). Бот = только из приложения (локального/маркет).
Вебхук умеет дёргать `imbot.*` лишь для уже существующего бота.

## Эндпоинты (Albery, через mcp.m4s.ru)
- `POST /bitrix/imbot/app` — обработчик **локального приложения** (боевой путь бота). Принимает
  события приложения с блоком `auth` (`access_token`/`application_token`/`client_endpoint`).
  Добавлен в `AUTH_EXEMPT_PREFIXES` и в редирект-исключения `canonical_web_redirect`.
- `POST /bitrix/imbot/<secret>` — старый sandbox-путь по вебхук-секрету (`B24_TESTBOT_SECRET`),
  оставлен, но через него бота зарегистрировать нельзя.

## Модель авторизации и состояние
- На `ONAPPINSTALL`/`ONAPPUPDATE` бот регистрируется (`imbot.register` через app `access_token` —
  тут CLIENT_ID есть, проходит), сохраняются `application_token` + `client_endpoint` + `bot_id`.
- Последующие события валидируются по `application_token` (`hmac.compare_digest`).
- Ответы/typing/like — через **per-event `access_token`** против `client_endpoint`
  (`{endpoint}/{method}.json?auth=<token>`). Токен живёт ~1ч — хватает на ответ из фонового потока.
- Состояние (application_token, bot_id, endpoint) в JSON-файле `/var/www/albery/.b24_testbot_state.json`
  (путь из `B24_TESTBOT_STATE`) — обработчику не нужен рестарт после установки.
- Бот зарегистрирован: `bot_id=24`, CODE `hermes_agent`, имя «Гермес-ассистент (тест)».

## Мост к Мозгу Гермеса
`_b24_app_process` зовёт `hermes_brain_answer()`:
```
hermes -z "<подсказка>+<сообщение>" --continue "bitrix-<dialog_id>" -t albery-faq --yolo
```
- `cwd=/root`, `HOME=/root` (albery-сервис крутится под **root**, поэтому видит `/root/.hermes`).
- Ответ берётся со stdout, таймаут `B24_TESTBOT_HERMES_TIMEOUT` (170с), сессия на диалог.
- Замер: ответ ~8–30с, пик памяти hermes-процесса ~160МБ (бокс 2ГБ + 2ГБ swap — тянет).

## Безопасность (важно)
- **Toolset жёстко ограничен** `-t albery-faq` — только read-only FAQ-MCP, БЕЗ `terminal/web/code`.
  Иначе сотрудник через чат мог бы выполнять команды на проде от root.
- Полный MCP (`albery` → `mcp.m4s.ru/mcp/...`) ходит в **боевой damaska** (создание/удаление задач,
  сообщения людям). Его даём только Евгению/Александру и отдельно/аккуратно — не для всех.

## MCP-коннекторы Гермеса (на 186, `/root/.hermes/config.yaml` → `mcp_servers`)
- `albery` — полный MCP (`mcp.m4s.ru/mcp/<secret>`), был.
- `albery-faq` — добавлен 2026-06-15, read-only (`mcp.m4s.ru/mcp-faq/<secret>`). Бэкап конфига
  `config.yaml.bak-faq`. Требует рестарта `hermes-gateway`.
- Мозг: `gpt-5.5` через `openai-codex`/OpenRouter (НЕ Gemini — ключ `GOOGLE_API_KEY` у Albery с
  квотой ~0, отсюда был 429 у интерим-цикла).

## Реакции
Старый `im.message.reaction` = `METHOD_NOT_FOUND`, НО богатые реакции идут через **IM v2**:
`im.v2.Chat.Message.Reaction.add` / `.delete` (принимает коды `eyes`, `like`, …). Логика:
- «прочитал» → 👀 (`eyes`, add) + индикатор `imbot.chat.sendTyping` («печатает…»);
- «выполнено» → снять 👀 (`delete`) и поставить 👍 (`like`, add) на сообщении пользователя.

## Tiered-доступ (по личности)
`B24_TESTBOT_FULL_USER_IDS` (default `14,16` = Евгений Палей / Александр Никитенко) → полный MCP
(`-t albery`, может действовать в боевой damaska, изменения подтверждает). Все остальные → `albery-faq`
(read-only). Системный промпт и tier пишутся в лог. На тест-портале id: Евгений=14, Александр=16,
владелец-бота «ИИ Агент»=22, админ katestone=1.

## Сессии (жизненный цикл) — `bitrix_bot_sessions` (миграция 028)
Автосжатие у Albery-Гермеса ОТКЛЮЧЕНО → контекст ограничиваем сами:
- сессия на диалог с эпохой: `bitrix-<dialog>-e<epoch>`;
- **простой >8ч** (`B24_TESTBOT_IDLE_RESET_SECONDS=28800`) → новая эпоха (свежий контекст);
- **лимит реплик** (`B24_TESTBOT_TURN_CAP=16`) → ротация эпохи с переносом КРАТКОЙ сводки
  (паттерн conversation-summary-buffer): сводку строит быстрый `hermes -z` по логу диалога.

### Ручной сброс «🆕 Новая сессия» (2026-06-16, миграция 030)
Пользователь может начать разговор заново. **Важный нюанс:** память бота инжектится из
`bitrix_bot_interactions` ПО `dialog_id`, а не по эпохе → одной смены эпохи мало. Поэтому добавлен
`bitrix_bot_sessions.history_floor_id` (миграция 030, в `ensure_postgres` ALWAYS_APPLY): сброс
(`_b24_session_reset`) поднимает эпоху И ставит floor = текущий `max(id)` интеракций; `_b24_recent_history`
тянет только строки `id > floor` → контекст реально чистый. Сброс идёт **без вызова модели**.
Триггеры (любой): кнопка **«🆕 Новая сессия»** под ответами (`_b24_reset_keyboard`, KEYBOARD у
`imbot.message.add`); зарегистрированная бот-команда **`/new`** (ленивый `imbot.command.register`,
видна в `/`-меню, клик → `ONIMCOMMANDADD`); набранные **ключевые слова** (`/new`,`/reset`,
«новая сессия»,«сбросить сессию»,… — `B24_RESET_TRIGGERS`, матч по ВСЕМУ сообщению, не сработает в
середине фразы). Набор `/new`/«новая сессия» работает гарантированно (проверено на проде:
floor выставляется, history после сброса = []); кнопка/команда — best-effort (зависят от портала,
не ломают ответ). Приветствие при входе в чат рекламирует сброс.

**Грабли (важно, проверено 2026-06-16):**
- `imbot.command.register` работает ТОЛЬКО через app `access_token` (per-event), НЕ через вебхук
  (вебхук → `403 ACCESS_DENIED "Client ID not specified"`, как и `imbot.register`). Регистрируем
  лениво на любом событии (`_b24_ensure_command_registered`, фоновый поток, флаг `cmd_new_registered`
  в state).
- **После регистрации `/new` приходит НЕ как сообщение, а как событие `ONIMCOMMANDADD`** с совершенно
  другим layout полей: `data[COMMAND][<idx>][COMMAND|COMMAND_ID|MESSAGE_ID|BOT_ID]`, а `DIALOG_ID` —
  в `data[PARAMS][DIALOG_ID]`. Обычный `_imbot_event_param` (читает `data[PARAMS][<name>]`) их не
  находит → ввели `_imbot_scan` (ищет по ХВОСТУ нормализованного ключа, `COMMAND` не цепляет
  `COMMAND_ID`). Симптом неправильного парсинга: бот возвращает 200, но не отвечает.
- **Команду ОБЯЗАТЕЛЬНО подтверждать** `imbot.command.answer` (COMMAND_ID) — иначе Bitrix считает её
  необработанной, **ретраит 3× и показывает вечное «печатает…»** (наблюдали 3 POST подряд). С ack —
  один чистый POST. Это и был «всё зависло» при первом тесте кнопки.

## Память: агент помнит свою прошлую работу (recall, 2026-06-18)
Симптом: агент «тупил» — переспрашивал `script_id`, говорил «не помню», заново создавал уже сделанные
приложения и присылал устаревшие ссылки (404 на старом deployment, хотя приложение живо — на актуальном
deployment 200). Корень: в промпт **НЕ инжектился `dialog_id`**, и агент **не знал, что может прочитать
свои прошлые шаги** — хотя инструменты `get_bitrix_bot_chat`/`list_bitrix_bot_sessions` (тиры admin/ops,
не faq) у него были. Амнезия by design.

Фикс (`hermes_brain_answer`): для admin/ops в промпт добавлен блок **«ПАМЯТЬ И КОНТЕКСТ»** с актуальным
`dialog_id` и железным правилом: прежде чем сказать «не помню» / просить у пользователя ссылку или
`script_id` / создавать заново — **СНАЧАЛА вызвать `get_bitrix_bot_chat(dialog_id=…)`** (полный
транскрипт со всеми прошлыми ссылками/`script_id`/`web_app_url`, в т.ч. из УЖЕ СБРОШЕННЫХ сессий) или
`list_bitrix_bot_sessions`. Плюс: создавая Apps Script/таблицу — всегда указывать в ответе `web_app_url`
+ `script_id` + `editor_url` (чтобы потом найти и переопубликовать именно его, а не плодить копии);
проверяя доступность — брать актуальный `web_app_url` из истории, не угадывать. `B24_TESTBOT_HISTORY_TURNS`
6→10 (глубже короткая память внутри сессии). Проверено: `get_bitrix_bot_chat('22')` отдаёт ~53k-символьный
транскрипт со ссылками script.google.com / spreadsheets. Важно: лог `bitrix_bot_interactions` для recall
**не режется history-floor** — агент видит всё, что делал. Коммит Albery `745c621`.

**Нюанс data-приложений:** веб-приложения, которые читают/пишут Google-таблицу, страницу отдают (doGet
200, открывается по ссылке), но сама запись в таблицу при `executeAs USER_DEPLOYING`+`ANYONE_ANONYMOUS`
может потребовать разовой авторизации владельцем `a9ent.ai` (headless-API не проходит consent) —
отдельная тема, не блокирует открытие ссылки. Чисто HTML-приложения работают полностью анонимно.

## Картинки (скрины) и ответы на сообщения — vision OCR + reply-контекст (2026-06-18)
Симптом: агент писал «не вижу вложенного скрина» и терял контекст при ответе (reply) на старое
сообщение. Выяснилось (логированием `B24_DEBUG_PAYLOAD=1`), что и картинка, и reply приходят **прямо
в событии `ONIMBOTMESSAGEADD`**; парсятся в `_b24_message_extras(payload)`:
- **Reply**: `data[PARAMS][REPLY_MESSAGE][MESSAGE]` = полный текст сообщения, на которое ответили →
  подмешивается в промпт как контекст. Идёт **мимо history-floor**, поэтому переживает сброс сессии.
  Ничего скачивать не нужно — работает сразу.
- **Картинки**: `data[PARAMS][FILES][<id>][...]` (`type`/`name`/`urlDownload`) + `…[PARAMS][FILE_ID][N]`.
  Инлайн-ссылки (`urlDownload`/`urlShow`/`unifiedLink`) **сессионные** — с сервера редиректят на
  OAuth-логин (302→login/HTML), скачать нельзя. Поэтому качаем серверно через REST `disk.file.get(id)` →
  `DOWNLOAD_URL`.
  - **❗ Качаем токеном БОТА, а не вебхука — и нужен scope `disk` у ЛОКАЛЬНОГО ПРИЛОЖЕНИЯ (исправлено
    2026-06-19, см. блок «Документы»).** Старый путь через вебхук `B24_TESTBOT_WEBHOOK_BASE` НЕ работает:
    его пользователь («ИИ Агент» id 22) **не участник** чатов бота → `disk.file.get` = **403 ACCESS_DENIED**
    даже со scope `disk`. Скачивание для картинок и документов теперь общее (`_b24_message_extras`).
  - OCR: `_b24_vision_ocr` → Groq `meta-llama/llama-4-scout-17b-16e-instruct` (env `B24_VISION_MODEL`),
    ключ `GROQ_API_KEY` из `/root/.hermes/secure/hermes-gateway.env` (albery под root). **UA-грабли:**
    `api.groq.com` через urllib даёт **Cloudflare 1010** на дефолтном UA → шлём `User-Agent: Mozilla/5.0`
    (та же грабля, что у STT, см. [hermes-operations.md](hermes-operations.md)).
  - Сообщение **только с картинкой** (без текста) больше не отбрасывается (`reason: empty`).
- Текст подмешивается через `_b24_compose_user_text`: `[Пользователь прислал изображение. Распознанное
  содержимое: …]` и `[Пользователь ОТВЕЧАЕТ на это более раннее сообщение …]`.
- Всё best-effort: любой сбой извлечения → бот просто отвечает как раньше. Коммит Albery `3701f88`.

### Документы (pdf/docx/xlsx/md/txt/csv) + ответ файлом PDF/Excel/Word (2026-06-18, **исправлено 2026-06-19**)
В `_b24_message_extras` файл качается через `disk.file.get → DOWNLOAD_URL`, затем извлекается текст
(`_b24_extract_document`): pdf → `pypdf`, docx → `python-docx`, xlsx → `openpyxl`,
md/txt/csv/json/html → как текст. Устаревший бинарный `.doc` не поддержан (нет libreoffice/antiword) —
агент честно попросит прислать PDF/DOCX. Текст подмешивается блоком `[Пользователь прислал документ
«…». Извлечённое содержимое: …]` (бюджет ~12k симв/док). Зависимости (`pypdf`,`python-docx`,`openpyxl`,
`reportlab`) — чистый python, в `requirements.txt`. Сообщение только с файлом больше не отбрасывается.

**❗ Скачивание — токеном БОТА, не вебхука (корневой фикс 2026-06-19, коммит Albery `3dde14f`).** Симптом
(тот самый баг владельца): пришлёшь `.docx` боту, спросишь «что в файле?» — а он **уверенно описывает
СТАРЫЙ файл** (напр. недавнюю Zoom-выгрузку) вместо нового. Причина: файл **молча терялся**, в мозг
уходил только текст «Что в этом файле?» (в БД `bitrix_bot_interactions` вопрос #106 = 17 символов), и
модель, имея Zoom-инструменты и упомянутые ранее даты, **галлюцинировала**. Почему терялся: качали через
входящий вебхук `B24_TESTBOT_WEBHOOK_BASE`, чей пользователь **не участник** чатов бота → `disk.file.get`
= **403 ACCESS_DENIED** (даже со scope `disk`; app-путь → 404 «REST V3 method not found»). Фикс:
- `_b24_app_download_url(endpoint, access_token, fid)` — `disk.file.get` через **per-event OAuth-токен
  приложения-бота** (`client_endpoint`); бот **участник** чата и файл видит. Вебхук оставлен fallback'ом.
- **Требует scope `disk` у ЛОКАЛЬНОГО ПРИЛОЖЕНИЯ** (НЕ у вебхука!): без него токен бота → `401
  insufficient_scope`. Добавлен владельцем на портале b24-0xrp3s 2026-06-19 (Разработчикам → Локальное
  приложение `hermes_agent` `local.6a30138eaaeee3…` → «Диск», переустановить). Инлайн `urlDownload` —
  сессионный ajax (`disk.api.file.download`, нужен cookie), токеном не открывается; серверно не годится.
- **Никогда не выдумывать (слой безопасности):** если файл есть, но скачать/распарсить не удалось —
  `_b24_message_extras` всё равно кладёт явный блок «не удалось прочитать файл, попроси переслать/PDF, НЕ
  придумывай содержимое» (раньше был молчаливый `continue` → галлюцинация). Проверено: после фикса тот же
  `.docx` дал **12 193 симв** реального текста в мозг.

**Ответ файлом — PDF / Excel / Word с НАСТОЯЩИМИ таблицами (2026-06-19, коммит `3dde14f`).** Раньше был
только плоский текст→PDF и владелец жаловался «ограничен встроенным экспортером, не делает таблицу как
прошу». Важно: мозг под `-t albery` имеет **только Albery-MCP** (Google Sheets/Docs/Apps Script, zoom),
**локального выполнения кода/записи файлов у него НЕТ** (probe → `CANNOT`), поэтому файлы рендерим
**детерминированно на стороне бота** из табличного текста модели:
- Маркеры `[[DELIVER_PDF: имя]]` / `[[DELIVER_XLSX: имя]]` / `[[DELIVER_DOCX: имя]]` (`_B24_DELIVER_RE` +
  `_B24_DELIVER_FMT`; `_b24_extract_deliver` → `(text, name, fmt)`). Промпт (`fmt`) учит: выбрать формат
  по просьбе («эксель»→xlsx, «ворд»→docx, иначе pdf), табличные данные оформлять Markdown-таблицей `|…|`,
  и брать **реальные данные из инструментов** (`search_tasks` и т.п.), а не выдумывать.
- `_b24_split_doc_blocks` парсит pipe-таблицы (строку-разделитель `|---|` отбрасывает) → реальные таблицы:
  PDF (`_b24_text_to_pdf`, reportlab `Table` с фиолетовым `#5440F6` заголовком), Excel (`_b24_text_to_xlsx`,
  openpyxl, жирный заголовок), Word (`_b24_text_to_docx`, python-docx, `Light Grid Accent 1`).
- `_b24_save_export(data, name, ext)` (обобщает `_b24_save_pdf_export`) → та же публичная токен-ссылка
  (zoom-export маршрут: HMAC(filename+expiry), TTL ~30 мин, `as_attachment`, mimetype по расширению,
  хост `mcp.m4s.ru`). Эмодзи вырезаются ТОЛЬКО в PDF (DejaVu не имеет глифов → «тофу»-квадратики; в чате
  и Excel/Word остаются). Сканы PDF без текстового слоя не распознаются (OCR-рендер страниц — отложено).
- Для произвольных/редактируемых документов мозг (ops/admin) умеет создавать **Google-таблицу/док** со
  ссылкой. Произвольную бинарную конвертацию на проде НЕ включали (риск OOM на боксе 2ГБ + безопасность —
  выполнение кода из чата от root); если понадобится — отдельный изолированный воркер.
- Прежние коммиты механизма: `559c546` (PDF-фича) + `35e23c7` (deps).

### Возможность «Создаю приложения» (эксклюзивный пункт, 2026-06-18)
В онбординг (шаг 1, `_b24_onboarding_text`) и в `ai_agent_capabilities` (tier `full`) добавлен
выделенный пункт: для сложных автоматизаций агент сам собирает веб-приложение на базе Google-таблиц
(`manage_apps_script publish_web_app`, см. [server-mcp-tools.md](server-mcp-tools.md)) — владелец
описывает идею/логику словами, агент пишет код, публикует (доступ по ссылке для всех) и присылает
готовый `web_app_url`. faq-тиру недоступно (только чтение).

## Аналитика
- `bitrix_bot_interactions` (миграция 027): строка на запрос (диалог, user, tier, вопрос, ответ,
  латентность, статус). Запись best-effort, не ломает ответ.
- **MCP-инструменты для агента (2026-06-16):** `list_bitrix_bot_sessions` (обзор: на каждого
  диалог/пользователя — имя, кол-во сообщений, tier, ошибки, эпоха, первая/последняя активность) и
  `get_bitrix_bot_chat(dialog_id|bitrix_user_id, [query/date])` (полный транскрипт вопрос→ответ для
  анализа качества). Раньше агент на вопрос «какие сессии с ИИ-агентом в Битриксе» отвечал «не было»
  — потому что лог ПИШЕТСЯ (027), но НИ ОДИН инструмент его не читал (агент смотрел в
  `chat_messages` = чаты между людьми). Инструменты — в ПОЛНОМ MCP, НЕ в `FAQ_TOOL_NAMES` (приватность:
  это переписка всех сотрудников → только владельцам). Имена резолвятся через вебхук бот-портала
  (`user.get`, кэш 1ч), т.к. таблица `users` синкается с другого портала (id не совпадают). После
  деплоя обязателен рестарт `hermes-gateway` (кэширует список tools) + `/reset` в TG.
- **Еженедельный дайджест** владельцу: `scripts/bitrix_bot_weekly_digest.py` → `hermes send --to
  "telegram:Александр Никитенко"` (цель в `B24_DIGEST_TARGET`), cron `/etc/cron.d/albery-bitrix-bot-digest`
  (Пн 10:00 МСК, `CRON_TZ=Europe/Moscow`), лог `/var/log/albery/bitrix-bot-digest.log`. Доставка проверена.

## Само-описание возможностей агента (tier-aware, 2026-06-16, миграция 031)
Чтобы агент сам знал/рассказывал, что умеет, и **отвечал по-разному для full и faq**:
- Таблица `ai_agent_capabilities(tier, content, updated_at, updated_by)` — строки `full`/`faq`,
  засеяны стартовым текстом (после сидинга контентом владеет агент/владелец, не миграция).
- **Почему БД, а не Google Doc:** агент может САМ дописывать БД через MCP, а доступ к Google у нас
  read-only (само-обновление в Doc невозможно) + лишняя зависимость синка.
- `get_ai_capabilities` — tier-aware через инжект `_connector_id` в `handle_request` (как у
  `start_here`): faq-коннектор видит ТОЛЬКО faq-список. Добавлен в `FAQ_TOOL_NAMES`.
- `update_ai_capabilities(content, mode=append|replace, tier=full|faq)` — **только full** (НЕ в FAQ),
  агент сам поддерживает список. `start_here` теперь направляет агента к `get_ai_capabilities` на
  вопрос «что ты умеешь». Проверено: faq/full отдают разное, update вне faq, само-обновление работает.

## Env (на 186, `/var/www/albery/.env`)
`B24_TESTBOT_WEBHOOK_BASE` (вебхук тест-портала, scope task/im/user/department/**imbot**),
`B24_TESTBOT_SECRET` (sandbox-путь), `B24_TESTBOT_MODEL` (gemini-2.0-flash, старый интерим-цикл),
`B24_TESTBOT_HERMES_TIMEOUT` (170с), `B24_TESTBOT_STATE`, `B24_TESTBOT_FULL_USER_IDS` (default `14,16`),
`B24_TESTBOT_IDLE_RESET_SECONDS` (28800), `B24_TESTBOT_TURN_CAP` (16), `B24_DIGEST_TARGET`.
`B24_DISCLAIMER` — текст сноски под КАЖДЫМ ответом (default «Ответы Албери AI могут быть неточными.
Проверяйте важную информацию.»; пустая строка = отключить).

**Оформление ответов (2026-06-16):**
- **Сноска-плашка** рендерится как **ATTACH** под ответом (отдельный блок с линией). ВАЖНО: REST-API
  Bitrix принимает ПЛОСКИЙ формат `ATTACH=[{"DELIMITER":{...}},{"MESSAGE":"..."}]`, а НЕ JS-формат
  `[{"COLOR":..,"BLOCKS":[..]}]` (тот даёт `ATTACH_ERROR "Incorrect attach params"`). В `_b24_app_reply`:
  сначала пробуем ATTACH-плашку, при ошибке — **инлайн `[i]...[/i]`** (сноска всегда видна) + лог
  причины. imbot.message.add с ATTACH идёт только через app-токен (вебхук = `ACCESS_DENIED`, нельзя
  оттестить оффлайн).
- **Структура ответа** — через промпт (`fmt` в `hermes_brain_answer`, оба тира): жирные заголовки
  `[b]Заголовок[/b]`, пункты «- », пустые строки между разделами, короткие ответы без оформления,
  без Markdown (Bitrix не рендерит `#`/`**`/таблицы; жирный — только `[b]`).

## Канал уведомлений в Битрикс24 «Albery Уведомления» (2026-06-22)
Аналог Telegram-группы «Albery_Уведомления» (-5283789593), **дублирует** её (TG остаётся). Сюда идут
**отчёты по кнопке «⚠️ Сообщить об ошибке»** + **еженедельные дайджесты** (жалобы и статистика бота).

**Чат:** `DIALOG_ID=chat730`, **принадлежит боту** (создан `imbot.chat.add` BOT_ID=24 → бот всегда
участник). Владелец (id 16) добавлен; остальных добавляют в UI. Старый `chat728` (создавался от
пользователя вебхука 22) **заброшен** — участников убрали, бот-пользователь вышел (в REST нет
`im.chat.delete` → `ERROR_METHOD_NOT_FOUND`; «удаление» = убрать всех людей).

**Постим ОТ ИМЕНИ БОТА** через `imbot.message.add`. Грабли и решение:
- `imbot.*` через входящий **вебхук** = `ACCESS_DENIED "Client ID not specified"` (как `imbot.register`).
  Бот постит только по **OAuth-токену приложения** (`access_token` ~1ч). Поэтому от пользователя вебхука
  (`im.message.add`, id 22) был промежуточный вариант — владелец отверг («странно, надо от бота»).
- Для крон-дайджеста живого события нет → **авто-рефреш**: `app_tokens`
  (access/refresh/expires/client_endpoint) хранятся в `.b24_testbot_state.json`; по `refresh_token`
  через `https://oauth.bitrix.info/oauth/token/` берётся новый токен + **ротируется refresh_token**.
  Каждый недельный крон рефрешит сам → цепочка живёт вечно (проверено: rotation + expiry +3600с).
- **Бутстрап:** первый `refresh_token` ловится из `auth`-блока ЛЮБОГО события бота (`_b24_capture_tokens`
  вызывается в начале `_bitrix_imbot_app_event`) — владелец один раз написал боту в личку.

**Креды приложения (только прод `/var/www/albery/.env`, root-only, НЕ в гите):**
`B24_TESTBOT_CLIENT_ID=local.6a30138eaaeee3.34345061`, `B24_TESTBOT_CLIENT_SECRET=…`,
`ALBERY_BITRIX_NOTIFY_CHAT=chat730` (в коде дефолт `chat728`).

**Код:**
- `app.py`: `_b24_capture_tokens`, `_b24_app_access_token` (рефреш+персист, race-safe),
  `_albery_bitrix_notify` → `imbot.message.add(BOT_ID,DIALOG_ID,MESSAGE)`. Отчёты об ошибках
  (`_b24_handle_error_report`) постят **по токену живого события** (рефреш не нужен).
- `scripts/b24_chat_notify.py` — standalone `notify()` для кронов (та же логика рефреша без импорта
  app.py; грузит `.env` через load_dotenv; кэш + reread при провале рефреша от гонки двух дайджестов).
- `scripts/error_report_digest.py` + `bitrix_bot_weekly_digest.py`: `bitrix_send` → `b24_chat_notify.notify`.
Коммиты Albery: `78f0ea6` (мирор, первая версия от пользователя), `4eac638` (переключение на бота +
рефреш), `e9242e7` (load_dotenv в `_access_token`). Все мирры best-effort — не ломают доставку в TG.

## Ключевые коммиты (репо Albery, ветка main)
- `819c2a7` sandbox imbot (Gemini+тулы) · `3e43ded` app-flow (регистрация на install, app-token) ·
  `f44e351` мост к Мозгу Гермеса (faq read-only) · `daa48b6` typing + 👍.
- `8db738d` — реконсиляция прод-онли правок (промпт недельного отчёта + метка канала), которые жили
  только на диске прода; теперь в git (источник истины восстановлен).
- `3dde14f` (2026-06-19) — чтение вложений токеном бота (нужен scope `disk` у локального приложения) +
  честный фолбэк вместо галлюцинации + ответ файлом в PDF/Excel/Word с настоящими таблицами.

## Деплой (бэкенд-only, правило #7)
Правка локально → commit/push → на 186 `cd /var/www/albery && git pull --ff-only && systemctl restart albery`.
БЕЗ `update_server.sh` (он пересобирает фронт = тяжело для памяти). Доступ к 186: креды в секрет-зоне
217 `/opt/hermes/secure/projects/albery/.env`; ходим local→217(paramiko)→186(sshpass).

## Реализовано 2026-06-15 (коммиты Albery)
`0d4a6a5` лог взаимодействий · `da62889` реакции 👀→👍 + tiered + сессии (миграции 027/028) ·
`03abf7e` еженедельный дайджест. Деплой бэкенд-only (`git pull` + `ensure_postgres.py` + рестарт albery).

## Open tasks
1. ✅ **Боевая миграция портала damaska → b24-0xrp3s ВЫПОЛНЕНА (2026-06-25).** `BITRIX_WEBHOOK_BASE`
   перенацелен с damaska на b24-0xrp3s (значение = `B24_TESTBOT_WEBHOOK_BASE`, rest/22), команда ре-синкнута
   на новые id (Артур 28, Наталья 30, Оксана 32, Олеся 34, Анастасия Андрусяк 42, Евгений 14, Александр 16),
   исходящие вебхуки task+team созданы в UI и проверены вживую (OnTaskAdd→синк, OnTaskDelete→очистка),
   `BITRIX_EVENT_SECRET` ротирован (старый засветился в логах + отрезает возможные damaska-вебхуки — handler
   авторизует только по секрету в URL). **Полный MCP бота теперь действует в b24-0xrp3s, НЕ в damaska** —
   full-tier (Евгений/Александр) меняет боевую систему на новом портале. Остаточные ручные шаги владельца:
   обновить URL 2 вебхуков на новый секрет; удалить мёртвый входящий damaska-вебхук в UI (не срочно).
   Детали и проверки — changelog 2026-06-25. **Известное расхождение ФИО** (мешает авто-диспетчу по имени):
   на портале «Софья Архипова» (id 36) vs в отчётах «Софья Погорелова»; «Дмитрий Строганов» (38) vs
   «Дмитрий Строгонов» — поправить ФИО на портале или завести алиас.
2. Опционально: кластеризация тем вопросов в дайджесте (сейчас просто последние вопросы), typing
   повторять каждые ~25с для длинных ответов, 👀-как-сообщение если v2-реакция где-то недоступна.
3. На тест-портале лог пустой до первых реальных чатов — дайджест наполнится после использования.
