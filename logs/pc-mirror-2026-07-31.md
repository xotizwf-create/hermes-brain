---
id: pc-mirror-2026-07-31
type: log
tags: [migration, github, backup, albery]
updated: 2026-07-31
secret_refs: []
---

# Полное зеркало рабочего ПК — 31.07.2026

Снимок подготовлен по схеме переноса от 18.07.2026: актуальные репозитории синхронизированы
с GitHub, незавершённые рабочие деревья сохранены в отдельных архивных ветках, полезные
игнорируемые артефакты добавлены в `hermes-brain`. Production-ветка Albery не смешивается
с экспериментами.

## Основные репозитории

- `xotizwf-create/Albery`, `main`: `c3766cb2053b494349ab15db41fcfd980ef37ef4`.
- `xotizwf-create/hermes-brain`, `main`: полный журнал, инструкции и переносимые рабочие
  артефакты; точный commit зеркала — следующий commit после создания этого файла.

## Незавершённые исходники

Сохранены в `xotizwf-create/Albery` как отдельные архивные ветки:

- `archive/pc-mirror-20260731-iu-p0-03-top` — `4400f6f`, ранний handoff WIP;
  профильная проверка: 10 passed.
- `archive/pc-mirror-20260731-custom-inbox-wip` — `65d46fb`, незавершённая версия durable
  custom inbox; профильная проверка: 48 passed.
- `archive/pc-mirror-20260731-iu-p0-03-audit-wip` — `711f837`, расширенный ранний аудит
  fallback/handoff; код компилируется, профильная проверка: 144 passed, 3 failed.
  Падения сохранены намеренно как часть незавершённого состояния и не относятся к `main`.
- `archive/pc-mirror-20260731-security-auth-hardening-20260730-local` — сохранён локальный
  указатель старой версии ветки, не перезаписывая одноимённую удалённую ветку.

Дополнительно опубликованы отсутствовавшие удалённые указатели локальных веток:
`bot-scenarios`, `iu-albery-quality`, `iu-form-merge`, `iu-form-token`, `iu-funnel-order`,
`iu-p0-02`, `iu-p0-03`, `iu-testing-mode`, `security/auth-hardening`.

## Полезные локальные артефакты

В зеркало `hermes-brain` форс-добавлены 46 игнорируемых файлов: скрипты развёртывания и
проверки калькулятора, готовые production bundles, снимки desktop/mobile, read-only
диагностические скрипты и вспомогательные результаты. Это переносимый архив работы,
а не новый production-контур.

Не включены:

- `.env` и любые реальные токены/пароли;
- локальные `.git` временных клонов;
- `__pycache__`, `.pytest_tmp*` и другие генерируемые тестовые файлы;
- `graphify-out` — воспроизводимый граф;
- устаревший отдельный клон `Albery-lead-cards`, в котором нет уникальных коммитов.

## Восстановление на другом ПК

```powershell
git clone git@github.com:xotizwf-create/Albery.git
git clone git@github.com:xotizwf-create/hermes-brain.git
git -C Albery fetch origin "+refs/heads/*:refs/remotes/origin/*"
```

Архивную ветку при необходимости открывать отдельно, не вливая в `main`:

```powershell
git -C Albery worktree add ..\Albery-iu-p0-03-archive `
  origin/archive/pc-mirror-20260731-iu-p0-03-audit-wip
```

Секреты восстанавливаются отдельно через защищённое хранилище; GitHub намеренно их
не содержит.
